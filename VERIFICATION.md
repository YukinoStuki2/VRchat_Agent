# 本地候选验收记录

阶段一可选扩展 `com.yukino.vrchat-managed-editing@0.1.0-preview.1`。
**本文件只记录管理机检查，不是Unity实机验收证明，也不是GitHub发布声明。**

## 实际执行

| 检查 | 结果与范围 |
|---|---|
| 纯 C# 授权/变更策略 | 15项通过；直接编译同一生产核心文件，不是伪造Unity程序集 |
| netstandard2.1 / C#9 核心兼容编译 | 0警告、0错误 |
| Unity扩展C#9语法解析 | 6个C#文件，0语法错误；不包含Unity类型/API绑定 |
| 源码边界与分发打包回归 | 25项通过，另24个参数子测试通过 |
| 受限入口真实回环HTTP | 25项通过；含nullable对象数组回归；上游为明确标识的测试服务，不是Unity |
| 当前真实MCP SDK | 1项通过：协议协商、列出17项工具、调用状态；上游仍是测试服务 |
| Linux真实启动入口 | 固定loopback 18082启动；GET被501拒绝，SIGINT正常退出，端口关闭、无stderr；未初始化上游，不等于Windows验收 |
| 原只读包 | v0.1.1的21个文件与基线逐字节一致；另在临时副本复跑原38项测试通过（仅将测试中的版本期望改为已发布0.1.1） |
| 敏感内容/危险代码模式扫描 | 审查分发清单内文件，未匹配本次扫描规则；不是完整安全证明 |

公开 `VERIFICATION.json` 仅保留实际检查摘要、独立复审范围及源码哈希；完整原始日志留在本地，不公开机器路径或诊断记录。source_sha256对应已审查的源码快照。GitHub安装与更新见 `GITHUB_INSTALL.md`。

## 已修复并重新检查

- 新客户端协议版本协商及空元数据兼容，仍只启用审核过的旧协议能力。
- nullable对象schema组合、所有工具结果形状校验，错误结果不作为正常响应透传。
- 精确上游会话清理、失败容量占用，避免前端过期映射不断产生不受控上游会话。
- 活动场景切换和新场景撤权；新方案清除旧预览，预览与方案/源快照绑定。
- 形态名称/顺序缓存更新；错误响应如实反映权限变化。
- Mesh支持范围收窄并明确局限，不宣称拥有全面几何内容哈希。
- 打包只选明确文件名；验证文件拒绝软链接、冻结原始证据字节，防止检查后换包。

## 未执行，仍必须验收

- Windows端脚本运行、真实Coplay工具注册、唯一目标项目绑定。
- Unity2022.3完整编译、面板实际布局与操作、模型预览渲染。
- 真模型的应用/回读、Undo、Prefab实例序列化、保存重开与恢复。
- 非均匀/负缩放、有骨骼/偏移rootBone模型的前后外观一致性。
- 真正的渲染PNG经Hermes原生视觉链路读取。
- Animator/眨眼/口型/MA/NDMF运行态是否覆盖捏脸。
- 本人Windows侧旧直通隧道的关闭与受限新入口替换。

本地验收阶段没有向原模型执行写入、没有安装到用户Unity工程、没有自动开权限、没有改Hermes配置。此前未进行Git发布；本分支另提供GitHub分发，不能据此视为Unity验收通过。当前仍是**临时工程验收候选包**。

首次安装请读 `START_HERE.md`，逐项执行 `Tests~/UNITY_ACCEPTANCE.md`；有错误就停止，不开放通用脚本工具来绕过。所有后续外部状态修改须另行明确授权与回读验证。
